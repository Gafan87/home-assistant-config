# yandex_smart_home.yaml
entity_config:
  binary_sensor.archer_a5_wan_status:
    name: "Домашний интернет"
    type: sensor
    properties:
      - type: event.open
        value_template: "{{ 'opened' if states('binary_sensor.archer_a5_wan_status') == 'on' else 'closed' }}"